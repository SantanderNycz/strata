{
  "buildCommand": "npx prisma migrate deploy --schema=server/prisma/schema.prisma && npm install --prefix client && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/graphql", "destination": "/api/graphql" },
    { "source": "/(.*)",    "destination": "/index.html"  }
  ]
}
